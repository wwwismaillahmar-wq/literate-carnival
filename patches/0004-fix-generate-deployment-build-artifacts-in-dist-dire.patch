From 05f776aaf8b63e96b3a5dd4c9705067bed78e43d Mon Sep 17 00:00:00 2001
From: ASLAN Bot <bot@aslan-modelling.local>
Date: Fri, 11 Sep 2026 09:48:42 +0000
Subject: [PATCH 4/4] fix: generate deployment build artifacts in dist
 directory

---
 .gitignore              |  2 ++
 package.json            |  2 +-
 scripts/prepare-dist.js | 39 +++++++++++++++++++++++++++++++++++++++
 3 files changed, 42 insertions(+), 1 deletion(-)
 create mode 100644 scripts/prepare-dist.js

diff --git a/.gitignore b/.gitignore
index 8fc0f5a..dfef09b 100644
--- a/.gitignore
+++ b/.gitignore
@@ -1,6 +1,8 @@
 node_modules
 .next
 out
+dist
+bun.lock
 .env*
 !.env.example
 .vercel
diff --git a/package.json b/package.json
index 4099dbb..6e4dad4 100644
--- a/package.json
+++ b/package.json
@@ -4,7 +4,7 @@
   "private": true,
   "scripts": {
     "dev": "next dev",
-    "build": "next build",
+    "build": "next build && node scripts/prepare-dist.js",
     "start": "next start",
     "lint": "next lint"
   },
diff --git a/scripts/prepare-dist.js b/scripts/prepare-dist.js
new file mode 100644
index 0000000..136281c
--- /dev/null
+++ b/scripts/prepare-dist.js
@@ -0,0 +1,39 @@
+const fs = require('fs');
+const path = require('path');
+
+const rootDir = path.resolve(__dirname, '..');
+const distDir = path.join(rootDir, 'dist');
+const nextDir = path.join(rootDir, '.next');
+const publicDir = path.join(rootDir, 'public');
+
+if (!fs.existsSync(distDir)) {
+  fs.mkdirSync(distDir, { recursive: true });
+}
+
+// Copy static app files if present
+const appServerDir = path.join(nextDir, 'server', 'app');
+if (fs.existsSync(appServerDir)) {
+  const indexPath = path.join(appServerDir, 'index.html');
+  if (fs.existsSync(indexPath)) {
+    fs.copyFileSync(indexPath, path.join(distDir, 'index.html'));
+  }
+  const notFoundPath = path.join(appServerDir, '_not-found.html');
+  if (fs.existsSync(notFoundPath)) {
+    fs.copyFileSync(notFoundPath, path.join(distDir, '404.html'));
+  }
+}
+
+// Copy _next/static to dist/_next/static
+const staticDir = path.join(nextDir, 'static');
+const distNextStatic = path.join(distDir, '_next', 'static');
+if (fs.existsSync(staticDir)) {
+  fs.mkdirSync(path.dirname(distNextStatic), { recursive: true });
+  fs.cpSync(staticDir, distNextStatic, { recursive: true });
+}
+
+// Copy public assets if public directory exists
+if (fs.existsSync(publicDir)) {
+  fs.cpSync(publicDir, distDir, { recursive: true });
+}
+
+console.log('Dist directory prepared successfully with Next.js build output.');
-- 
2.34.1

